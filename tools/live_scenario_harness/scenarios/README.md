# Scenario Fixtures

Scenario directories are generated under `/tmp/hyprland-settings-live-scenarios/<timestamp>/` by `tests/live_scenario_harness.rs`.

They are not committed because they are temporary write fixtures.
