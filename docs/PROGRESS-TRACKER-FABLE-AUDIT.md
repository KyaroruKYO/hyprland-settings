# Progress Tracker — Fable Audit (2026-07-13)

Machine-readable result: `data/reports/progress-tracker-fable-audit.v0.55.2.json`.

## How this audit was generated

Generated by Fable (Claude Code) by **direct repository interrogation**,
not copied from any prior tracker, prompt wording, or external estimate:

- git state read with git commands: branches, tags, remote refs.
- The full test suite executed: **262 test binaries, 1050 tests passed,
  0 failed, 17 ignored** (env-gated live proofs).
- Counts taken from **test-regenerated reports** (the runtime-preview
  capability matrix, the dead-man classification, the input/cursor
  live-proof status) and from **source receipt constants**
  (`PROVEN_RECORD_SHAPE_PROOFS` = 4, `PROVEN_INPUT_ROWS` = 37,
  `SAFE_WRITABLE_ROWS` = 341 entries).
- Release state verified with `sha256sum` over all three `dist/` trees and
  `gh release view v0.2.0`.
- Manual-test state from `data/reports/manual-rc-test-v0.2.0-rc.1.v0.55.2.json`.

Percentages below are counted fractions from those sources; no number was
inherited from earlier trackers.

## Audit target

- Commit audited: `0ffaeb3` (**Release v0.2.0**), branch
  `structured-family-editors-unified`; `main` fast-forwarded to the same
  commit.
- Tags: `v0.1.0` → 74efa28 (untouched); `v0.2.0-rc.1` → 82cf18c (annotated,
  pushed); `v0.2.0` → 0ffaeb3 (annotated, pushed).

## State by area (status — evidence)

| Area | Status | Evidence |
| --- | --- | --- |
| Release | **release complete** | GitHub release v0.2.0 published (3 assets, not draft/prerelease, `gh release view`); dist trees checksum-verified; v0.1.0 untouched |
| Validation | all green | 262 binaries / 1050 tests / 0 failures / 17 env-gated ignored; fmt, jq, diff-check, release build, appstream, desktop-file all pass |
| Manual RC test | passed for the driven surface | 10/10 driven steps on the packaged binary in the real session; the two config-writing clicks stay human-only (app policy) with their paths live-flow-proven |
| Scalar settings | complete for the modeled surface | 341/341 rows modeled, classified, and gated-save-writable |
| Runtime preview | complete for proven surface | 135/341 rows live-previewable (~40%: 62 direct, 73 throttled); 78 dead-man; 43 config-write-only; 74 blocked high-risk; 11 not proven yet (capability matrix, regenerated each run) |
| Dead-man preview | complete for proven surface | 38/78 armed (~49%); 35 of 63 input/cursor rows promoted by passed proofs (37 receipts); 26 disarmed: 18 touch hardware, 3 secondary-device judgment, 5 model-only; scroll_method blocked by a proof finding; timeout auto-revert re-proven through the packaged binary |
| Structured families | complete for proven surface | 7/7 classified; animation + curve editable for proven shapes; monitor/bind/device/permission/gesture blocked with rendered reasons |
| Record picker | complete for proven surface | 4 zero-residue shape receipts (speed, enabled both directions, bezier existing-curves-only, curve points); combined save gated on all three animation receipts; creation/deletion inexpressible; style blocked (no trusted valid-value evidence) |
| Safe Live Save Mode | complete | live-verified gate on every Save; UI toggle proven on the packaged binary; user-chosen persistence live-flow-proven |
| Source/include + duplicate | capped: approved but default-disabled | decision reviews reach ApprovedButDefaultDisabled, executors unwired, GTK-asserted disabled cards |
| High-risk / display | blocked | 74 blocked high-risk rows; no display recovery design exists |
| Profile/mode switching | blocked | blocked by production activation + live proof; review-only card |
| Hyprland 0.55.4 | complete (zero drift) | 341 = 341, 0 added/removed/changed, 0 bounds drift vs the trusted official export; repeatable read-only refresh |
| Hardware-gated proofs | deferred | 18 touch + 3 secondary-device rows; 0 promoted; virtual path proposal-only, not proven |
| UX / readability | **not proven yet** | rendering and interactions are proven, but the Config page mixes developer/proof cards with user controls and no usability pass exists — the honest open quality gap |

## Known stale docs / historical sections

- The long `data/reports` tail is frozen history; only test-regenerated
  reports plus those dated 2026-07-12/13 reflect current state.
- `docs/PROGRESS-TRACKER.md` keeps dated historical sections (logs).
- `docs/FINISH-APP-SPRINT.md`, `RELEASE-BOUNDARY-APPROVAL-PROMPT.md`:
  historical sprint artifacts.
- `docs/RELEASE-DECISION.md` / `docs/RELEASE-CANDIDATE-v0.2.0-rc.1.md`:
  resolved by the v0.2.0 release; each carries a resolution note.

## Next exact work

1. Hardware-gated proofs when the devices exist (18 touch + 3
   secondary-device rows).
2. A UX simplification/readability pass separating end-user controls from
   developer/proof surfaces — the one unproven quality area.
3. Style editing only if trusted valid-value evidence appears (then live
   proof first).
4. Track future Hyprland releases with
   `tools/refresh_hyprland_descriptions_export.sh`.
